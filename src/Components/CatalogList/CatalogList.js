import React from 'react';
import Card from "../TopSale/Card";

const CatalogList = ({list}) => {
    return (
        <div className="catalog-list _container">

        </div>
    );
};

export default CatalogList;